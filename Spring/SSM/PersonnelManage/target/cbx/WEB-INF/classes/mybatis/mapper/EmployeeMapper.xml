<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.EmployeeMapper">

    <select id="findInEmp" resultType="com.domain.Employee">
        select * from employee where isdelete=0 and name like '%${name}%' and
                                     departmentName like '%${departmentName}%' and gender like '%${gender}%'
    </select>

    <select id="findOneEmp" resultType="com.domain.Employee">
        select * from employee where id = #{id}
    </select>

    <select id="findOutEmp" resultType="com.domain.Employee">
        select * from employee where name like '%${name}%' and departmentName like '%${departmentName}%'
                                       and gender like '%${gender}%' and del_type like '%${del_type}%'
    </select>

    <insert id="addInEmp" parameterType="com.domain.Employee">
        insert into employee values (DEFAULT,#{name},#{gender},#{email},#{departmentName},
                                    #{tel},#{indate},#{bz},NULL,0,'')
    </insert>

    <update id="updateEmp" parameterType="com.domain.Employee">
        update employee
        set
        name=#{name},
        gender=#{gender},
        email=#{email},
        departmentName=#{departmentName},
        tel=#{tel},
        bz=#{bz}
        where id = #{id}
    </update>

    <update id="deleteInEmp" parameterType="com.domain.Employee">
        update employee
        set
        isdelete=1,
        outdate=#{outdate},
        del_type=#{del_type}
        where id = #{id}
    </update>

</mapper>