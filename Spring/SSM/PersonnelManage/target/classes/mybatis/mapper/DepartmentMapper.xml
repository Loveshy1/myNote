<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.DepartmentMapper">
    <select id="findDep" resultType="com.domain.Department">
        select * from department where departmentName like '%${departmentName}%' and leader like '%${leader}%'
    </select>

    <insert id="addDep" parameterType="com.domain.Department">
        insert into department values (#{id},#{departmentName},#{leader},#{detail},0)
    </insert>

    <update id="updateDep" parameterType="com.domain.Department">
        update department
        set
        departmentName=#{departmentName},
        leader=#{leader},
        detail=#{detail}
        where id = #{id}
    </update>

    <update id="deleteDep" parameterType="int">
        update department set isdelete=1  where id = #{id}
    </update>

</mapper>